import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  emptyContainer: {
    marginTop: 48,
    alignItems: "center",
  },
  emptyTitle: {
    fontWeight: "bold",
    fontSize: 14,
    color: "#808080",
    marginTop: 10,
    textAlign: "center",
  },
  emptySubtitle: {
    textAlign: "center",
    fontWeight: "normal",
  },
});
